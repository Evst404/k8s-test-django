apiVersion: v1
kind: Service
metadata:
  name: django
  namespace: edu-aleksandr-evstigneev
  labels:
    app: django
spec:
  type: ClusterIP
  selector:
    app: django
    tier: web
  ports:
  - name: http
    port: 80
    targetPort: 80
